node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.vite/

# Personal brainstorming / working notes — not part of the public project
notes/

# Runtime game state (game in progress, roster, play history)
data/state.json

# Uploaded images. Kept out of the repo deliberately: they'd bloat it, and
# publishing sourced images under this project's license isn't ours to do.
# Share them with an exported pack instead — see docs/development.md.
data/images/

# Screenshots from `npm run shot`
screenshots/
